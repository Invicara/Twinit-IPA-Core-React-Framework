{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap --no-ci",
    "build": "lerna run build",
    "build-dev": "lerna run build-dev",
    "release": "echo Building... && npm run build && lerna version --exact",
    "quickrelease": "lerna version --exact",
    "changed": "lerna changed",
    "publish": "lerna bootstrap --no-ci && lerna run build && lerna publish from-package --yes",
    "preinstall": "rm -rf /var/lib/jenkins/workspace/vicaraPlatformApplication_master/packages/ipa-core/node_modules"
  },
  "devDependencies": {
    "@rollup/plugin-image": "^2.0.6",
    "@svgr/rollup": "^5.5.0",
    "enzyme": "^3.11.0",
    "enzyme-adapter-react-16": "^1.15.6",
    "lerna": "^3.19.0",
    "postcss-assets": "^6.0.0"
  }
}
